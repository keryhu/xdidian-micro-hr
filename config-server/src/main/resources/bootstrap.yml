
spring:
  application:
    name: config-server
    
encrypt:
  fail-on-error: false
  # 如果写成key－store: 则会出错。
  keyStore:
    location: classpath:microserver.jks
    password: jsk8iiu2e # -storepass 密码 
    alias:  serverconfig
  


 

